{
  "name": "wexa-graph-app",
  "version": "1.0.0",
  "description": "Developer Skills Network – A graph database application built with CognoDB",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "postinstall": "npm install --prefix frontend && npm run build --prefix frontend",
    "dev": "nodemon server.js",
    "seed": "node seed/seed.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "neo4j-driver": "^6.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
